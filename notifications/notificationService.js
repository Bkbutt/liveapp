// Function to simulate sending notifications (replace this with your actual notification system)

exports.sendNotification = (userId, message) => {
  console.log(`Sending notification to user with ID ${userId}: "${message}"`);
  // Implement your notification logic here, e.g., push notification, email, SMS, etc.
};